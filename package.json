{
  "name": "todo-app-fullstack",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && python -m uvicorn main:app1 --reload",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && pip install -r requirements.txt && cd db && python create_database.py",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}